# Copy these values into Netlify's environment-variable settings (or a local
# .env file when running `npm run dev:mp`). Never expose NCB_SECRET_KEY to the
# browser or commit a real value.
NCB_INSTANCE=56358_wheres_the_line
NCB_DATA_API_URL=https://openapi.nocodebackend.com
NCB_SECRET_KEY=replace-with-your-nocodebackend-secret-key
